---
import LanguageSwitcher from "./LanguageSwitcher.astro";
import { useTranslations, localizePath, type Lang } from "@i18n/utils";

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useTranslations(lang);

const links = [
  { href: localizePath("/", lang), label: t("nav.home") },
  { href: localizePath("/race-info", lang), label: t("nav.race_info") },
  { href: localizePath("/anmeldung", lang), label: t("nav.signup") },
  { href: localizePath("/startliste", lang), label: t("nav.startlist") },
  { href: localizePath("/galerie", lang), label: t("nav.gallery") },
  { href: localizePath("/ergebnisse", lang), label: t("nav.results") },
  { href: localizePath("/faq", lang), label: t("nav.faq") },
  { href: localizePath("/kontakt", lang), label: t("nav.contact") },
];

const current = Astro.url.pathname.replace(/\/+$/, "") || `/${lang}`;
---

<header class="site-header" id="site-header" data-header>
  <div class="container header-inner">
    <a class="brand" href={localizePath("/", lang)} aria-label={t("site.title")}>
      <img
        src="/images/Logo.jpeg"
        alt=""
        class="brand-logo"
        width="40"
        height="40"
        loading="eager"
        decoding="async"
      />
      <span class="brand-name">Dolomites Last Loop</span>
    </a>

    <nav class="primary-nav" aria-label="Primary">
      <ul>
        {
          links.map((link) => {
            const normalized = link.href.replace(/\/+$/, "") || `/${lang}`;
            const isActive =
              normalized === current ||
              (normalized !== `/${lang}` && current.startsWith(normalized));
            return (
              <li>
                <a
                  href={link.href}
                  class:list={{ active: isActive }}
                  aria-current={isActive ? "page" : undefined}
                >
                  {link.label}
                </a>
              </li>
            );
          })
        }
      </ul>
    </nav>

    <div class="header-actions">
      <LanguageSwitcher lang={lang} />
      <button
        class="burger"
        type="button"
        aria-label={t("nav.menu")}
        aria-expanded="false"
        aria-controls="mobile-nav"
        data-burger
      >
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <div
    class="mobile-nav"
    id="mobile-nav"
    role="dialog"
    aria-modal="true"
    aria-label={t("nav.menu")}
    hidden
  >
    <nav aria-label="Mobile">
      <ul>
        {
          links.map((link) => (
            <li>
              <a href={link.href}>{link.label}</a>
            </li>
          ))
        }
      </ul>
      <div class="mobile-lang">
        <LanguageSwitcher lang={lang} />
      </div>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    inset: 0 0 auto 0;
    z-index: 50;
    height: var(--nav-height);
    background: transparent;
    transition:
      background var(--transition),
      border-color var(--transition),
      box-shadow var(--transition);
    border-bottom: 1px solid transparent;
  }

  .site-header[data-scrolled="true"] {
    background: var(--color-bg);
    border-bottom-color: var(--color-border);
    box-shadow: 0 2px 16px rgba(45, 74, 107, 0.06);
  }

  /* On pages where the hero is dark (full-bleed), nav text starts white */
  .site-header:not([data-scrolled="true"]) .primary-nav a,
  .site-header:not([data-scrolled="true"]) .brand-name {
    color: #ffffff;
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.5);
  }
  .site-header:not([data-scrolled="true"]) .burger {
    border-color: rgba(255, 255, 255, 0.6);
  }
  .site-header:not([data-scrolled="true"]) .burger span {
    background: #ffffff;
  }
  .site-header:not([data-scrolled="true"]) .lang-link {
    color: rgba(255, 255, 255, 0.75);
  }
  .site-header:not([data-scrolled="true"]) .lang-link.active,
  .site-header:not([data-scrolled="true"]) .lang-link:hover {
    color: #ffffff;
  }
  .site-header:not([data-scrolled="true"]) .sep {
    color: rgba(255, 255, 255, 0.35);
  }

  /* Pages without a dark hero still need legible nav */
  .site-header[data-solid="true"] {
    background: var(--color-bg);
    border-bottom-color: var(--color-border);
  }
  .site-header[data-solid="true"] .brand-name,
  .site-header[data-solid="true"] .primary-nav a {
    color: var(--color-text) !important;
    text-shadow: none !important;
  }

  .header-inner {
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-5);
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    color: var(--color-text);
  }
  .brand:hover {
    color: var(--color-primary);
  }
  .brand-logo {
    height: 40px;
    width: 40px;
    object-fit: cover;
    border-radius: 6px;
    background: #ffffff;
  }
  .brand-name {
    font-family: var(--font-display);
    font-size: 1.3rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    transition: color var(--transition);
  }

  .primary-nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: 0.25rem;
  }

  .primary-nav a {
    display: inline-block;
    padding: 0.6rem 0.85rem;
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    border-radius: var(--radius-sm);
    transition: color var(--transition);
    position: relative;
  }

  .primary-nav a::after {
    content: "";
    position: absolute;
    left: 0.85rem;
    right: 0.85rem;
    bottom: 4px;
    height: 2px;
    background: var(--color-primary);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform var(--transition);
  }
  .primary-nav a:hover::after,
  .primary-nav a.active::after {
    transform: scaleX(1);
  }
  .primary-nav a.active {
    color: var(--color-primary);
  }
  .site-header:not([data-scrolled="true"]) .primary-nav a.active::after {
    background: #ffffff;
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }

  .burger {
    display: none;
    background: transparent;
    border: 1px solid var(--color-border-strong);
    width: 42px;
    height: 42px;
    border-radius: var(--radius-sm);
    padding: 0;
    cursor: pointer;
    position: relative;
  }
  .burger span {
    display: block;
    width: 18px;
    height: 2px;
    background: var(--color-text);
    margin: 4px auto;
    transition: transform var(--transition), opacity var(--transition);
  }
  .burger[aria-expanded="true"] span:nth-child(1) {
    transform: translateY(6px) rotate(45deg);
  }
  .burger[aria-expanded="true"] span:nth-child(2) {
    opacity: 0;
  }
  .burger[aria-expanded="true"] span:nth-child(3) {
    transform: translateY(-6px) rotate(-45deg);
  }

  .mobile-nav {
    position: fixed;
    inset: var(--nav-height) 0 0 0;
    background: var(--color-bg);
    padding: var(--space-7) var(--space-5);
    overflow-y: auto;
    z-index: 49;
  }
  .mobile-nav ul {
    list-style: none;
    margin: 0 0 var(--space-6);
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }
  .mobile-nav a {
    display: block;
    padding: var(--space-3) 0;
    font-family: var(--font-display);
    font-size: 1.6rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text);
  }
  .mobile-lang {
    margin-top: var(--space-6);
  }

  @media (max-width: 1024px) {
    .primary-nav,
    .header-actions > .lang {
      display: none;
    }
    .burger {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
    }
  }
</style>

<script>
  const header = document.querySelector<HTMLElement>("[data-header]");
  const burger = document.querySelector<HTMLButtonElement>("[data-burger]");
  const mobileNav = document.getElementById("mobile-nav");

  // If the page has no .hero element, render the header in solid mode immediately
  const hasHero = !!document.querySelector(".hero");
  if (header && !hasHero) {
    header.dataset.solid = "true";
  }

  function setScrolled() {
    if (!header) return;
    header.dataset.scrolled = window.scrollY > 24 ? "true" : "false";
  }
  setScrolled();
  window.addEventListener("scroll", setScrolled, { passive: true });

  burger?.addEventListener("click", () => {
    const open = burger.getAttribute("aria-expanded") === "true";
    burger.setAttribute("aria-expanded", String(!open));
    if (mobileNav) {
      mobileNav.hidden = open;
      document.body.style.overflow = open ? "" : "hidden";
    }
  });

  mobileNav?.querySelectorAll("a").forEach((a) =>
    a.addEventListener("click", () => {
      burger?.setAttribute("aria-expanded", "false");
      if (mobileNav) mobileNav.hidden = true;
      document.body.style.overflow = "";
    }),
  );
</script>
